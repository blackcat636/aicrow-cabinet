/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'storage.uncar.us',
      'api.uncar.us',
      'localhost',
      'img.heroui.chat'
    ]
  }
};

export default nextConfig;
